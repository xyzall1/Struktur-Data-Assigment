#include <iostream>
using namespace std;

int main() {
    int = 6;
    int data [n] = {1, 2, 3, 4, 5, 5};
    int cari = 2;
    bool ketemu = false;
    int i ;

    for (i = 0; i < n: i++) {
        if (data [i] == cari) {
            ketemu = true;
            break;
        }
    }

    cout << "\t Pencarian sequential search"\n << endl;
    cout << "data : {1, 2, 3, 4, 5, 5}" << endl;
        if (ketemu) {
    cout << \n "angka "  << cari << "ketemu di indeks ke-" << i << endl;

    } else {
    cout << cari << "ga ketemu" endl;

    }
    return 0;
}